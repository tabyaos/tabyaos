# Local assistant/API credentials. Copy to .env and fill locally.
# Never commit .env or real credentials.

GEMINI_API_KEY=

# Optional AWS variables for local Packer validation/builds.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_DEFAULT_REGION=us-east-1
