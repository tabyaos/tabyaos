---
galaxy_info:
  role_name: cis_k8s_worker
  author: tabyaos
  description: CIS Kubernetes Benchmark v1.8 — Worker Node section (kubelet, container runtime, PKI)
  license: BSL-1.1
  min_ansible_version: "2.14"

dependencies:
  - role: cis-l2
