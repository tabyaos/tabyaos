---
- name: Converge — chrony-hardened
  hosts: all
  become: true
  gather_facts: true

  vars:
    chrony_additional_servers: []
    chrony_manage_services: false  # No systemd in Docker

  roles:
    - role: chrony-hardened
      tags: [chrony, pci_dss]
